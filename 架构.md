# 文档处理架构：结构化解析与父子分块

本文是待实施的设计规格，不表示以下能力已经全部接入代码。保留原有多解析器、TableIR、表格检索与数值计算路线，采用无重叠的结构切分；仅在结构单元超出 Token 上限时进行长度兜底切分，并对同一超长单元的连续子片使用有限重叠，同时保留父子分块。

第一版核心实现、配置方法及尚未落地的部分见 [文档处理第一版](docs/document-processing-v1.md)。项目处于开发阶段且无存量文档，统一使用新模式，不保留旧切分、旧向量存储或旧检索模式。以下仍是完整目标规格，不能将多解析器路由等后续能力视为已经全部实现。

核心约定：**子块用于检索，父块用于按需补充上下文；完整原文与结构化数据始终保留。**

## 1. 文档解析与入库

                         用户上传文件
                              │
                              ▼
                     Go 文件服务 / MinIO
                              │
                    SHA-256、大小、权限检查
                              │
                              ▼
                   Apache Tika 通用预处理
                   ├─ MIME 类型检测
                   ├─ 文件元数据提取
                   ├─ 是否包含嵌入附件
                   └─ 通用格式初步解析
                              │
              ┌───────────────┴─────────────────┐
              │                                 │
              ▼                                 ▼
        非 PDF 文件                          PDF 文件
              │                                 │
              ▼                                 ▼
      Tika 作为主解析器                    页面级质量诊断
      ├─ DOC/DOCX                         ├─ 原生文字质量
      ├─ PPT/PPTX                         ├─ 图片覆盖率
      ├─ HTML/RTF                         ├─ 多栏/表格/公式
      ├─ EML/MSG                          └─ 是否疑似跨页结构
      └─ 其他异构文件                           │
              │                   ┌─────────────┼──────────────┐
              │                   │             │              │
              │                   ▼             ▼              ▼
              │          原生文字可靠       简单扫描页      复杂扫描页
              │                   │             │              │
              │                   ▼             ▼              ▼
              │              Docling       PP-OCRv6    PaddleOCR-VL
              │            do_ocr=false
              │                   │             │              │
              │                   ├──────┐      │      ┌───────┤
              │                   │      │      │      │       │
              │                   ▼      │      │      ▼       ▼
              │           原生表格低质量 │      │   跨页/复杂表格
              │                   │      │      │      │
              │                   ▼      │      │      ▼
              │                Camelot   │      │    MinerU
              │                          │      │
              └──────────────────────────┴──────┴──────────────┐
                                                               ▼
                                                      统一 Document IR
                                                               │
                                      ┌────────────────────────┼──────────────┐
                                      │                        │              │
                                      ▼                        ▼              ▼
                                  正文段落                  表格结构        元数据
                                  标题层级              Markdown/HTML      附件关系
                                  页码/BBox                 JSON
                                      │
                                      ▼
                           结构化父子分块
                          ┌───────────┴───────────┐
                          ▼                       ▼
                   子块 300～600 tokens    父块 1000～2000 tokens
                   仅超长续片重叠          原文上下文，不生成向量
                          │                       │
                          ▼                       ▼
                   Qwen3-Embedding          MySQL / MinIO
                          │                       │
                          ▼                       │
                   Elasticsearch Bulk ── parent_id ┘




## 2. 表格处理与查询

                              PDF
                               │
                               ▼
                       页面与表格检测
                               │
             ┌─────────────────┼──────────────────┐
             │                 │                  │
             ▼                 ▼                  ▼
     原生文字表格        扫描表格          跨页复杂表格
             │                 │                  │
             ▼                 ▼                  ▼
        Docling          PaddleOCR-VL          MinerU
             │
       低质量时Camelot
             │                 │                  │
             └─────────────────┴──────────────────┘
                               │
                               ▼
                         统一 TableIR
                               │
                 ┌─────────────┴─────────────┐
                 │                           │
                 ▼                           ▼
             结构校验                    表头展开
             候选选择                    类型识别
             跨页合并                    单位解析
                 │                           │
                 └─────────────┬─────────────┘
                               ▼
              ┌────────────────┼───────────────────┐
              │                │                   │
              ▼                ▼                   ▼
           MinIO             MySQL            Elasticsearch
       JSON/HTML/图片    表格、单元格及父块   summary/row_group
                                                  │
                                                  ▼
                                      BM25 + Vector + RRF
                                                  │
                                                  ▼
                                           子块 Reranker
                                                  │
                                                  ▼
                                  父块去重、鉴权及按预算补充上下文
                                                  │
                                ┌─────────────────┴──────────────┐
                                │                                │
                                ▼                                ▼
                           普通表格问答                     数值计算问题
                                │                                │
                                ▼                                ▼
                           LLM直接回答                      Go/MySQL计算
                                │                                │
                                └────────────────┬───────────────┘
                                                 ▼
                                          带页码和行列引用
表格先保留 JSON、HTML 和截图，完成结构校验、多级表头展开、类型/单位解析，再生成检索表示。跨页合并必须校验表头、列结构、标题及页面连续性，不能仅因列数一致就合并；保留各单元格的原始页码和位置。

Camelot、MinerU 是质量不足时的候选/回退路径，不要求每份文件依次运行所有引擎。原生 PDF、扫描 PDF 和混合 PDF 按页面诊断路由；跨页表格按相关页组处理。具体引擎版本和适配输出在实施前锁定。

## 3. 统一数据模型

### 3.1 DocumentIR / TableIR

- 文档：`document_id`、`content_version`、`pipeline_version`、`owner_id`、文件名、SHA-256、原文件位置、解析器及版本。
- 内容块：`block_id`、`type`、`text`、`heading_path`、阅读顺序和 `source_spans`。
- 类型至少区分标题、段落、列表项、表格、代码、公式、图片说明；附件保留独立身份及父文档关系，不混入正文。
- 来源：`source_spans` 保留一个或多个页面/BBox及块内字符区间；跨页块不能只存单个页码。统一页码、坐标原点、比例与旋转转换。没有可靠页码或位置时保存空值，不编造定位信息。
- 表格：`table_id`、表名、表头路径、行列坐标、合并行列数、原始文本、规范化值、单位、来源与质量标记。

`heading_path` 按识别出的章节树生成；短标题关联后续正文，不单独生成无内容向量。去除确认的重复页眉页脚，但保留有意义的脚注及其关联。结构识别不可靠时退化为段落/列表边界，并标记来源，不让模型补造标题层级。

### 3.2 ParentChunk / ChildChunk

父块与子块共同保存：

| 字段 | 含义 |
| --- | --- |
| `chunk_id`、`parent_id` | 块身份与父子关系；父块的 parent_id 为空 |
| `document_id`、`owner_id`、`content_version` | 所属文档、权限主体与内容版本 |
| `pipeline_version`、`chunker_version` | 解析及分块规则版本，包含 tokenizer/配置版本关联 |
| `kind`、`heading_path`、`file_name` | 内容类型、完整章节路径和展示文件名 |
| `body_text`、`embedding_text` | 可回溯正文与检索表示；父块无需生成 embedding_text |
| `source_spans`、`block_ids` | 原文位置与 IR 内容块映射 |
| `token_count`、`tokenizer_id`、`tokenizer_revision` | 最终序列化后的 Token 计数及其依据 |
| `prev_id`、`next_id` | 同结构范围内的相邻块，用于受控扩展 |
| `table_id`、`row_range`、`column_paths` | 表格块的来源范围；普通正文为空 |
| `overlap_spans` | Token 限长兜底续片与前一子片重复的原文区间；结构切分时为空，用于去重和引用 |

第一版采用一层父子关系：每个子块只属于一个父块，子块原文范围必须位于父块内。父块是原文集合，不是 LLM 摘要。身份以文档/版本/块定位信息构成，不能只用文件名，也不能把内容哈希当作权限凭据。

## 4. 正文分块规则

### 4.1 默认长度与边界

下列数值是初始规格，需用业务样本评测，而不是所有文档都必须填满的配额。

| 项目 | 初始值 | 规则 |
| --- | --- | --- |
| 子块目标 | 450 tokens | 通常 300～600；最终序列化后上限 600，目标值不触发结构单元内部切分 |
| 结构切分重叠 | 0 | 在标题、段落、列表项等结构边界结束，不复制上一块正文 |
| Token 兜底子片重叠 | 75 tokens | 仅同一超长结构单元的连续子片使用；优先完整句子，通常 50～100，最多 100，必要时更少或为 0 |
| 父块目标 | 1500 tokens | 通常 1000～2000；最终序列化后上限 2000，目标值不触发结构单元内部切分 |
| 父块重叠 | 0 | 默认不重复大块原文，通过相邻块关系恢复边界上下文 |

300 / 1000 是软目标，不是最小长度强制值。短章节、尾块、短列表允许小于目标，不为凑长度合并无关章节。

Token 始终用于长度计量与校验，但不是默认切分方式。先寻找可用的结构边界；结构单元连同必要前缀不超过相应上限时保持完整，恰好达到上限也无需再拆。只有超限且无法继续按结构细分时，才进入 Token 限长兜底；不能为了凑 450 / 1500 tokens 强行切开完整单元。结构切分即使是因容量不足而在段落边界换块，也不加重叠。

### 4.2 先父块，再子块

1. 从 DocumentIR 读取章节树和阅读顺序，按标题 → 段落 → 列表项建立原文单元。
2. 优先在同一叶章节内聚合父块；同一章节过长时，按完整段落/列表项拆成多个父块，不加重叠。单个原文单元连同必要前缀超过 2000 tokens 且无更细结构边界时，才按句子/安全字符边界分成可容纳的连续片段，再分配到父块；父块仍不重叠，原文不丢失。原则上不跨叶章节合并，不跨文档、附件、权限或版本边界。
3. 在每个父块内部生成子块，按完整段落/列表项等结构边界切分或聚合，可参考 450 tokens 选择已有边界。若追加下一个完整单元会超过 600 tokens，就在两者的结构边界结束当前块，新块不复制前块正文；单个单元能装入空块时，不将其强行切开填满前块。
4. 仅当单段、列表项等结构单元连同必要前缀超过 600 tokens，且已无可用的更细结构边界时，才启动 Token 限长兜底切分。兜底时优先按句子落刀；单句仍超长时，再按 tokenizer 可映射到原文的安全字符边界继续拆分。
5. 所有拆分都保留剩余内容：不能把“Token 截断”实现为丢弃超出部分。每个原文单元必须由对应子块/续片的 `source_spans` 联合完整覆盖，扣除重叠后不缺失正文；特殊超长单元也必须保留到完整 IR。
6. 仅对步骤 4 因 Token 超限拆出的同一原文单元的连续子片，从前一片末尾选取有限重叠，优先完整句子，计入下一片的 600-token 上限；若无法容纳，优先减少重叠，不裁掉新增正文。每一片必须向前推进，不能生成纯重叠片或死循环。普通结构切分不执行此步骤。
7. 兜底重叠还必须位于同一父块、同一章节、同一原文单元内部；不跨章节/父块/段落/列表项复制正文，回到下一个结构单元时停止重叠。结构边界或父块边界需要更多上下文时，通过受控邻块扩展处理。
8. 普通代码块、公式、列表项尽量保持完整；超长时按代码行或可解释的子结构拆分，保留语言/列表说明/原始定位，不把表格送入普通句子切分器。

### 4.3 Token 计数与检索文本

- 分块使用与所选 Qwen3 Embedding 模型一致、固定 revision 的 tokenizer；不得以字符数、字节数、字符数除以常数冒充实际 Token 数。
- `embedding_text` 可包含必要的文件名、标题路径和正文；长度校验必须覆盖实际发给模型的全部字符串、重叠及必要特殊 token，而不是只数 body_text。
- 超长元数据前缀要有单独的预算策略；可以缩短检索表示中的展示前缀，但结构化元数据仍保留完整标题路径和文件名，不能挤掉正文。
- 父块长度采用同一 tokenizer 计量，以便一致验收。最终进入问答 LLM 时，需要按该 LLM 的 tokenizer 重新核算消息、记忆、资料和输出预留；缺少 tokenizer 时只能使用明确标注的保守估算并保留余量。
- 模型相关 Token 计数/长段细分优先复用解析 Worker 已加载的 tokenizer，按批处理；Go 保留结构规则、父子关系、元数据校验和编排，避免每一句跨网络调用或重新加载模型。

Docling 的 HybridChunker 可以复用其结构化、token-aware 细分与 metadata contextualize 能力，但不能将它视为本规格的父子关系、重叠、版本/权限控制已自动完成。其他引擎先转换为统一 IR，再应用相同分块契约。[官方分块说明](https://docling-project.github.io/docling/concepts/chunking/)、[tokenizer 与元数据计数示例](https://docling-project.github.io/docling/_generated/examples/hybrid_chunking/)

## 5. 表格分块规则

表格按行、列等结构切分，不套用普通正文的 Token 兜底重叠。必要表头/主键的重复属于定位前缀，不是相邻数据块的正文重叠。

1. 小表：序列化后的表名、必要表头、单位和内容若不超过 600 tokens，可作为一个子块。1000 tokens 以下的完整表也允许直接成为较短父块。
2. 大表：以完整行组成 `row_group` 子块，仍以 300～600 tokens 为通常目标。10～30 行仅是参考，不是硬性切分条件；宽表可能几行就达到上限。
3. 每个行组重复完整、必要的表头路径、单位和行标识，这些前缀同样计入预算。结构切分不重复数据行；确实需要邻行时，在检索阶段按原始 row IDs 受控扩展并去重，不在入库分块时复制相邻数据行。
4. 单行超过 600 tokens 时，按列组拆分，重复必要的主键/行标题并保留列路径；单个单元格本身超长时，用带同一 cell ID 的文本续片表示。完整行、原始数值和单元格关系仍留在 TableIR，不能静默裁剪。
5. 表格父块为整表或连续行区间，通常 1000～2000 tokens。超大表拆为多个父块，不能为满足“一表一个父块”突破上限；`table_id` 仍指向完整表。
6. 每张表保存 `table_summary` 检索表示，帮助定位表格；它不是原始数值证据，也不要求映射到整张超大表的单一父块。可生成/提取描述，但所有事实必须能回查 TableIR。
7. 父块和子块的重复表头/行标识属于检索上下文，不得在数值计算时计为额外数据。跨页原始 row/cell ID 和 source_spans 保持不变。
8. `fact` 索引延后按实际问题引入，避免一开始同时生成多套重复索引。数值 max/min/sum/average/count 从完整授权范围的数据计算，不从 Top-K 行组或表格摘要计算。

## 6. 存储与索引

| 存储 | 内容 |
| --- | --- |
| MinIO | 原文件、完整 IR、页图、表格 HTML/截图、可选较大的父块原文产物 |
| MySQL | 文档/任务状态与活动版本、父块及子块映射、表格和可计算单元格数据 |
| Elasticsearch | 子块与 table_summary 的全文/向量、parent_id、来源、文档/处理版本及权限过滤字段 |

第一版父块不额外向量化、不与子块同时竞争检索排名；通过 parent_id 批量取回即可。整表的权威数据是 TableIR/MySQL，不能只存在 ES 的 row_group 文本中。

Qwen3 Embedding 必须明确具体模型和输出维度。例如 Qwen3-Embedding-0.6B 支持最高 1024 维；文档和查询使用一致的模型、维度和预处理。若切换模型或维度，应新建索引、重新生成向量、验证后切别名，不混用旧模型向量。[官方模型卡](https://huggingface.co/Qwen/Qwen3-Embedding-0.6B)

分块/模型/解析版本变化时，建立新版本的父块、子块、表格与索引，完成校验后发布活动版本。失败保留旧的可检索版本，不能先删旧块再生成半套新数据。批量 Embedding、ES Bulk 都要逐项验证结果。

## 7. 查询与父块扩展

```text
当前问题 + 有界会话记忆
  → 指代消解 / 独立问题 / 有界拆解
  → 带权限与活动版本过滤的子块 BM25 + Vector
  → 双路 RRF，再融合各子问题结果
  → 子块 Reranker，保留多跳桥接证据及各分支必要来源
  → 近重复/重叠去重，按 parent_id 分组
  → 批量鉴权取回父块，按相关性和剩余预算选择扩展
  → 普通知识解释，或结构化表格查询 / Go / MySQL 计算
  → 最终上下文预算校验
  → 回答与精确来源定位
```

父块扩展规则：

- 不是召回几个子块就无条件加载几个完整父块。优先保障命中子块、各子问题覆盖和多跳桥接引文，再补需要解释的父块上下文。
- 预算裁剪顺序为：可选父块/邻块扩展 → 低相关非必要子块；始终保护桥接引文和各分支必要来源。必要证据本身仍放不下时明确要求缩小范围，不悄悄删掉一个子问题。
- 多个子块命中同一父块时，父块只注入一次，但保留全部有效命中证据；父块排序可先取该父块最高子块分数，不因重叠子块多而重复加分。
- 选取父块时保护不同子问题/文档来源的覆盖。不能让一个父块下的多个相似子块挤掉比较问题另一侧的证据。
- 如果整个父块不适合剩余预算，退回命中子块与邻近完整原文单元，不直接截取父块开头，也不裁掉桥接引文、行标识或表头。邻块补充只在同文档/版本及合理章节范围内进行，并设置有界数量/Token 预算。
- 父块和邻块读取都按当前用户、document_id、活动版本重新鉴权；不能仅凭客户端或 ES 返回的 parent_id 任意读取。
- 上下文按 source_spans 合并重叠区间，避免同时注入完整父块及已被覆盖的同一子块原文。展示来源编号只在最终证据集合确定后生成。
- 文件页码、表格行列引用应锚定实际使用的子块/原始单元格，而不是只引用整个父块。新增父块片段若提供新证据，也必须获得对应 source_spans 和引用；没有坐标就明确降级为页/表定位。
- 数值查询先定位表、列和筛选范围，再读取该范围内完整数据执行白名单聚合；仅允许校验后的结构化请求和参数化查询，不执行 LLM 自由生成的 SQL。处理单位、空值、合计行与重复行。
- 文档内容、表格、历史及父块均为不可信资料，不提升为系统指令；用户记忆不是文档权限依据，也不是文档数值事实来源。

## 8. 任务、权限与发布底座

保留 Go、Kafka、MySQL、Redis、MinIO、ES，增加常驻解析 Worker，不新增另一套任务框架。

- 上传完成与解析/索引可用分离：`queued → parsing → normalizing → chunking → embedding → indexing → ready`，另记 failed/partial、进度、失败原因和重试次数。只有 ready 的活动版本可检索。
- 使用稳定文档 ID；MD5 用于上传完整性检查，服务端增加 SHA-256 校验。任务携带 job_id、文档/处理版本，执行前及发布前检查是否过时或已删除。
- 限制实际上传大小、页数、解压/嵌入附件数量、Worker CPU/GPU/内存/时间；隔离不可信文件解析，限制外部资源访问；HTML 预览进行安全处理。
- 明确有界重试、退避、失败留存和人工重试；不能将“不提交 Kafka offset”当作当前任务的显式重试循环。
- 删除文档先使其不可检索、使旧任务失效，再按 owner/document/version 清理原件、IR、页图、父子块、SQL 表格和 ES。重试不得复活已删除产物。
- 对非 PDF 的页码/章节能力按实际解析输出保留；不能从 Tika 的纯文本反向恢复已经丢掉的准确页码和表格结构。需要这些信息的格式使用结构输出或专门适配。

## 9. 与当前项目的改造对应

以下为完整目标的模块对应；已实现的核心与后续能力以第一版说明为准。

| 位置 | 改造内容 |
| --- | --- |
| `internal/model` | DocumentIR/TableIR、父子块、来源定位及版本字段 |
| `internal/service/upload_service.go`、`pkg/tasks/tasks.go`、`pkg/kafka/client.go` | 输入校验、任务状态/版本、有界重试和恢复 |
| `pkg/document/client.go`、解析 Worker | 统一结构化解析输出与 tokenizer 适配；通用格式由 Worker 调用 Tika |
| `internal/pipeline/processor.go` | 唯一的结构化管线；串联父块/子块校验及版本发布 |
| `internal/repository` | 父块按 ID 批量取回、父子版本一致性、表格数值与删除清理 |
| `pkg/embedding/client.go`、`pkg/es/client.go` | 一致模型/维度、批量向量化与 Bulk、新索引及别名迁移 |
| `internal/service/search_service.go`、`query_execution.go` | 子块双路召回/RRF/Reranker、父块分组、多分支及多跳证据保护；同步替换依赖绑定输入中固定截取前 1000 字符的规则 |
| `internal/service/chat_service.go`、`conversation_memory.go`、`context_budget.go` | 父块扩展后的全量预算、来源重新编号；替换会切掉证据的固定 1000 字符片段规则 |
| 前端来源展示与文档预览 | 文件/页/章节/表格行列定位；预览读取已发布版本 IR |

当前 `context_budget.go` 使用 UTF-8 字节数作为保守估计，不能直接作为这版“300～600 tokens”的精确计数器；可以继续作为最终问答预算的降级保护，但必须区分实际 Token 和估计值。

## 10. 实施与验收顺序

1. **准备基线样本并冻结契约**：选择原生、扫描、混合、跨页表格、长列表/代码等脱敏样本，标注章节、页码、单元格及问答证据；固定 IR、Tokenizer、父子关系和来源映射规格。
2. **补可靠底座**：任务状态/版本、输入校验、重试、活动版本发布与删除防复活；避免处理变重后放大现有失败风险。
3. **接通结构化解析**：先跑通 Docling 和一条扫描解析路径，保存统一 IR、表格结构和来源；按图逐步接入其他引擎的质量兜底。
4. **实现父子分块**：目标 450/1500 tokens，结构切分重叠为 0；仅对超出子块上限、无法继续结构细分的单元做 Token 兜底，续片重叠约 75 tokens。落实章节边界、表格专用规则、真实 Token 校验和完整覆盖。
5. **构建索引并接通查询**：子块向量化/Bulk，父块批量取回，RRF/Reranker、去重、有界扩展与预算；从新上传的基线样本验证。
6. **接入计算和精确引用**：表格完整范围计算、页/行列定位，验证权限变化与来源一致性。
7. **按样本评测调整参数和引擎**：比较 300/450/600-token 子块目标，以及仅子块/父子扩展的证据召回率、答案正确性、引用准确率、冗余率、Token 开销和延迟；结构切分始终保持 0 重叠，仅在超长单元的 Token 兜底样本中比较 50/75/100-token 重叠。没有测量前不声称某一组参数最优。

确定性验收必须包括：

- 普通子块序列化后不超过 600 tokens、父块不超过 2000；结构短块可以不足目标。
- 按标题/段落/列表项边界生成的相邻块无正文重叠，`overlap_spans` 为空；因追加完整单元会超限而换块时也不例外。
- 单个结构单元序列化后为 450、599 或恰好 600 tokens 时不启动 Token 兜底；超过 600 且无更细结构边界时才拆分，重叠和前缀仍计入每片上限。父块同样验证 2000-token 边界。
- 原文覆盖完整，长句/长单元格不丢尾部；重叠不会无限循环或制造纯重复块。
- 子块原文包含于父块，父子 owner/document/version 匹配；重叠仅限同一超长单元的连续子片，不跨父块、章节、段落或列表项。
- 同父块只加载/注入一次，重叠不重复计分或计算，多分支与多跳桥接引文不丢失。
- 父块扩展后仍满足最终上下文预算；依赖绑定和最终回答两个入口都不能再按开头 1000 字符随意裁掉命中证据。
- 表头、单位、行标识完整，超宽表/跨页表的定位和去重可靠；整表统计不依赖 Top-K 数据子集。
- 重新解析减少块数、任务重复、解析失败、删除/权限变更期间的旧任务均不产生可检索残留。
- 页码/BBox/字符区间映射可回查原文，包含旋转页、多栏、跨页块及无可靠坐标的降级路径。

第一版不引入多层无限递归父块、父块重复向量索引或额外 Agent 框架。保留扩展路径，先验证“一层父子 + 精确定位 + 有界上下文”能否满足真实问题。
